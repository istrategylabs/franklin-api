db:
  image: postgres
web:
  build: .
  env_file: .env
  command: python manage.py runserver 0.0.0.0:8000
  working_dir: /code/franklin
  volumes:
    - .:/code
  ports:
    - "8000:8000"
  links:
    - db
